<h2 class="ballon_header">{{properties.place|raw }}</h2>
<a class="ballon_body" data-id="{{properties.index}}" id="place-link" href="#">{{properties.address|raw }}</a>
<div class="ballon_body">{{properties.place|raw }}</div>
<div class="ballon_body">{{ properties.name|raw }}</div>
<div class="ballon_body">{{ properties.description|raw }}</div>
